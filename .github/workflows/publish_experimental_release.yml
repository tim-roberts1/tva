on:
  push:
    branches:
      - main

jobs:
  build-packages:
    runs-on: ubuntu-latest
    name: 👷‍♀️ Build all packages
    env:
      RELEASE_CHANNEL: 'experimental'
    steps:
      - uses: actions/checkout@v3.0.2
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
          cache: 'yarn'
      - run: |
          yarn install
          yarn prepare:flags
          yarn prepare:release --release experimental --commit=$GITHUB_SHA --ci --run=$GITHUB_RUN_ID
          yarn build:packages
          tar -cvf packages_dist.tar packages
      - name: Archive production artifacts
        uses: actions/upload-artifact@v3
        with:
          name: packages
          path: packages_dist.tar

  experimental-release:
    runs-on: ubuntu-latest
    name: 🚀 Publish experimental release
    needs: build-packages
    env:
      RELEASE_CHANNEL: 'experimental'
    steps:
      - uses: actions/checkout@v3.0.2
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
          cache: 'yarn'
      - uses: actions/download-artifact@v3
        with:
          name: packages
          path: dist
      - run: |
          yarn config set -H 'npmAuthToken' "${{ secrets.NPM_TOKEN }}"
          yarn install
          yarn prepare:flags
          yarn prepare:release --release experimental --commit=$GITHUB_SHA --ci --run=$GITHUB_RUN_ID
          yarn publish:packages --ci --tags experimental
