---
tags: []
title: 'Button'
---

import DocsLinkBar from '@site/src/components/DocsLinkBar/DocsLinkBar'
import LiveExampleTabs from '@site/src/components/LiveExampleTabs/LiveExampleTabs'

<p className="size-xxl">
  Used to perform an action, such as submitting a form, opening/closing a
  dialog, or using a cancel action.
</p>

<DocsLinkBar
  github="https://github.com/pluralsight/pando/tree/main/packages/headless-styles/src/components/Button"
  figma="https://www.figma.com/file/ZmH4XsZS5WnKeo28ylM5x1/Pando-UI-kit-(Web)?node-id=1214%3A50560&t=YLA3FBYLefNvfeuf-0"
/>

## Anatomy

1. Leading Icon (optional)
2. Label
3. Trailing Icon (optional)

## Import

```javascript
import { getButtonProps } from '@pluralsight/headless-styles'
```

```javascript
import type { ButtonOptions } from '@pluralsight/headless-styles/types'
```

## Usage

### Basic Buttons

```jsx live
function BasicButtons() {
  const gridItem = getGridItemProps({ colSpan: 2 })

  return (
    <div {...getGridProps({ cols: 12, gap: 8 })}>
      <div {...gridItem}>
        <button {...getButtonProps().button}>Action</button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ sentiment: 'default' }).button}>
          Default
        </button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ sentiment: 'danger' }).button}>
          Danger
        </button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ usage: 'outline' }).button}>
          Outline
        </button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ usage: 'text' }).button}>Text</button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ disabled: true }).button}>Disabled</button>
      </div>
    </div>
  )
}
```

### Outline Buttons

```jsx live
function OutlineButtons() {
  const gridItem = getGridItemProps({ colSpan: 2 })

  return (
    <div {...getGridProps({ cols: 12, gap: 8 })}>
      <div {...gridItem}>
        <button {...getButtonProps({ usage: 'outline' }).button}>Action</button>
      </div>

      <div {...gridItem}>
        <button
          {...getButtonProps({ sentiment: 'default', usage: 'outline' }).button}
        >
          Default
        </button>
      </div>

      <div {...gridItem}>
        <button
          {...getButtonProps({ sentiment: 'danger', usage: 'outline' }).button}
        >
          Danger
        </button>
      </div>

      <div {...gridItem}>
        <button
          {...getButtonProps({ usage: 'outline', disabled: true }).button}
        >
          Disabled
        </button>
      </div>
    </div>
  )
}
```

### Text Buttons

```jsx live
function OutlineButtons() {
  const gridItem = getGridItemProps({ colSpan: 2 })

  return (
    <div {...getGridProps({ cols: 12, gap: 8 })}>
      <div {...gridItem}>
        <button {...getButtonProps({ usage: 'text' }).button}>Action</button>
      </div>

      <div {...gridItem}>
        <button
          {...getButtonProps({ sentiment: 'default', usage: 'text' }).button}
        >
          Default
        </button>
      </div>

      <div {...gridItem}>
        <button
          {...getButtonProps({ sentiment: 'danger', usage: 'text' }).button}
        >
          Danger
        </button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ usage: 'text', disabled: true }).button}>
          Disabled
        </button>
      </div>
    </div>
  )
}
```

### Button with icon

```jsx live
function ButtonWithIcon() {
  const gridItem = getGridItemProps({ colSpan: 3 })
  const { button, iconOptions } = getButtonProps({
    icon: 'start',
  })
  const { button: trailButton } = getButtonProps({
    icon: 'end',
  })

  return (
    <div {...getGridProps({ cols: 12, gap: 16 })}>
      <div {...gridItem}>
        <button {...button}>
          <PlaceholderIcon {...getIconProps(iconOptions)} />
          Leading icon
        </button>
      </div>

      <div {...gridItem}>
        <button {...trailButton}>
          Trailing icon
          <PlaceholderIcon {...getIconProps(iconOptions)} />
        </button>
      </div>
    </div>
  )
}
```

### Sizes

```jsx live
function ButtonSizes() {
  const gridItem = getGridItemProps({ colSpan: 2 })

  return (
    <div
      data-site-override="alignGridCenter"
      {...getGridProps({ cols: 12, gap: 16 })}
    >
      <div {...gridItem}>
        <button {...getButtonProps({ size: 'm' }).button}>Medium</button>
      </div>

      <div {...gridItem}>
        <button {...getButtonProps({ size: 'l' }).button}>
          Large (default)
        </button>
      </div>
    </div>
  )
}
```

#### Size Mapping

```typescript title="Button size rules."
{
  m: 'Use in condensed areas where the default size is too large.',
  l: 'Should be used in most, if not all cases.'
}
```

## Reference

```typescript
getButtonProps(options?: ButtonOptions)
```

### Parameters

```typescript
interface ButtonOptions {
  classNames?: string[]
  disabled?: boolean
  sentiment?: 'default' | 'action' | 'danger'
  usage?: 'filled' | 'outline' | 'text'
  size?: 'm' | 'l'
  icon?: 'start' | 'end'
}
```

### Returns

1. An Object matching the [Anatomy](#anatomy) containing `className` and a11y properties relevant to each element/section including an unused class consisting of the naming pattern `pando-btn`.
2. An `iconOptions` Object to use as the parameters for the Icon API.

## Behavior

### Patterns

**Do** use the Button for an actionable event needed from the user.

**Don't** use the Button for navigation or styled links. Instead use the TextLink component.

**Do** keep Button usage minimal within an area.

**Don't** use more than three Buttons in a single area.

**Do** space Buttons apart by 16px (1rem).

**Don't** space Buttons apart greater or less than 16px (1rem).

### Sentiment

**Do** use the "action" sentiment for all use cases where possible.

**Don't** use the "default" sentiment _unless_ it is a **last resort**.

**Do** use the "danger" sentiment for cancel or destructive actions.

**Don't** use the "default" sentiment for cancel or destructive actions.

### Usage

**Do** use the "outline" usage for a **lower priority alternative** use case to the "default" sentiment.

**Don't** use the "outline" usage for cancel or destructive actions.

**Do** use the "text" usage for the **lowest** priority action.

**Don't** use the "text" usage as a high-priority action.

### Sizing

**Do** use the default size for all use cases where possible of the Button.

**Don't** use the "m" size _unless_ it is a **last resort**.

## Accessibility

The Pando Button, and Icon APIs combined with semantic HTML allow the Button to be fully accessible and screen-readable.
