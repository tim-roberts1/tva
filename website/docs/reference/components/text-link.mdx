---
tags: [breadcrumb, text button, link]
title: 'Text Link'
---

import {
  DocsLinkList,
  DocsGithubLink,
  DocsFigmaLink,
} from '@site/src/components/DocsLinkBar/DocsLinkBar'
import LiveExampleTabs from '@site/src/components/LiveExampleTabs/LiveExampleTabs'

<p className="size-xxl">Used to navigate the user to a new page.</p>

<DocsLinkList>
  <DocsGithubLink href="https://github.com/pluralsight/pando/tree/main/packages/react/src/components/TextLink.tsx" />
  <DocsGithubLink href="https://github.com/pluralsight/pando/tree/main/packages/headless-styles/src/components/TextLink/textLinkCSS.ts">
    View Core API
  </DocsGithubLink>
  <DocsFigmaLink href="https://www.figma.com/file/ZmH4XsZS5WnKeo28ylM5x1/Pando-UI-kit-(Web)?node-id=1214%3A50560&t=YLA3FBYLefNvfeuf-0" />
</DocsLinkList>

## Import

```javascript
import { TextLink } from '@pluralsight/react'
```

## Anatomy

1. Link
2. External Icon (optional)

## Usage

### Basic Links

```jsx live
function BasicTextLinks() {
  const gridItem = getGridItemProps({ colSpan: 3 })
  return (
    <div
      data-site-override="alignGridCenter"
      {...getGridProps({ cols: 12, gap: 16 })}
    >
      <div {...gridItem}>
        <TextLink href="/">Local Text Link</TextLink>
      </div>

      <div {...gridItem}>
        <TextLink href="https://github.com/pluralsight/pando">
          External Text Link
        </TextLink>
      </div>
    </div>
  )
}
```

## Behavior

### Usage

**Do** use the Text Link when you need to provide a way to navigate to a different page.

**Don't** use a Button to navigate the user to a different page.

**Do** use the ExternalLink Icon when you have a link that will navigate the user outside of your application/site.

**Don't** eliminate the ExternalLink Icon _unless_ the link uses a local path.

## API

### Parameters

```typescript
type TextLinkProps = AnchorHTMLAttributes<HTMLAnchorElement>
```

1. Any property that can be passed to an HTML `<a>` element.

## Accessibility

The Pando TextLink, and Icon APIs combined with semantic HTML allow the TextLink to be fully accessible and screen-readable by default.

For external links, the `target` attribute is set to "\_blank" and the `rel` attribute is set to "noopener noreferrer" to prevent tab-nabbing.
