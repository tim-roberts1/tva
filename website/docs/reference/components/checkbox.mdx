---
tags: [multi-select, field, form]
title: 'Checkbox'
---

import CheckboxStates from '@site/src/components/Checkbox/CheckboxStates'
import DocsLinkBar from '@site/src/components/DocsLinkBar/DocsLinkBar'
import LiveExampleTabs from '@site/src/components/LiveExampleTabs/LiveExampleTabs'

<p className="size-xxl">
  Used when a user needs to select multiple values from options in a form.
</p>

<DocsLinkBar
  github="https://github.com/pluralsight/pando/tree/main/packages/headless-styles/src/components/Checkbox"
  figma="https://www.figma.com/file/ZmH4XsZS5WnKeo28ylM5x1/Pando-UI-kit-(Web)?node-id=1214%3A50560&t=YLA3FBYLefNvfeuf-0"
/>

## Import

```javascript
import { getCheckboxProps } from '@pluralsight/headless-styles'
```

```typescript
interface CheckboxOptions {
  checked: boolean
  disabled?: boolean
  id: string
  indeterminate?: boolean
  invalid?: boolean
  name: string
  readOnly?: boolean
  required?: boolean
  value: string
}

getCheckboxProps(options?: CheckboxOptions)
```

```javascript
import type { CheckboxOptions } from '@pluralsight/headless-styles/types'
```

## Anatomy

1. Container
2. Control
3. Input
4. Label

## Usage

### Basic Checkbox

```jsx live
function BasicCheckbox() {
  const [checked, setChecked] = useState(false)
  const checkbox = getCheckboxProps({
    checked,
    id: 'sign_up',
  })

  function handleChange() {
    setChecked((prev) => !prev)
  }

  return (
    <FormControlProvider>
      <div {...checkbox.checkboxContainer}>
        <input {...checkbox.input} onChange={handleChange} />
        <span {...checkbox.checkboxControl}>
          <Show when={checkbox.input.checked} fallback={null}>
            <Icon icon={CheckIcon} size="s" />
          </Show>
        </span>
        <Label htmlFor="sign_up">Sign up for something</Label>
      </div>
    </FormControlProvider>
  )
}
```

### Indeterminate Checkbox

```jsx live
function IndeterminateCheckbox() {
  const checkbox = getCheckboxProps({
    id: 'sign_up',
    indeterminate: true,
    checked: true,
  })
  const icon = getIconProps(checkbox.iconOptions)

  return (
    <FormControlProvider>
      <div {...checkbox.checkboxContainer}>
        <input {...checkbox.input} onChange={() => null} />
        <span {...checkbox.checkboxControl}>
          <Show when={checkbox.input.checked} fallback={null}>
            <Icon icon={IndeterminateIcon} size="s" />
          </Show>
        </span>
        <Label htmlFor="sign_up">Sign up for something</Label>
      </div>
    </FormControlProvider>
  )
}
```

:::tip use the pando hook

To easily determine the "indeterminate" state, use the [useIsIndeterminate custom hook](../react-hooks/useIsIndeterminate.mdx).

:::

```jsx showLineNumbers title="How to use the useIsIndeterminateHook." {7,12-13}
const [allSelected, setAllSelected] = useState(false)
const [checkboxOptions, setCheckboxOptions] = useState({
  email: false,
  sms: false,
  phone: false
})
const isIndeterminate = useIsIndeterminate(checkboxOptions)

...

// The select-all Checkbox
<Checkbox
  checked={allSelected || isIndeterminate}
  indeterminate={isIndeterminate}
  ...
/>
```

### States

<CheckboxStates />

:::note Form field and state

In order to easily manage the state and maintain Accessibility of your form field, the basic state statuses should come from the result of the FormControl API.

:::

## Reference

```typescript
getCheckboxProps(options?: CheckboxOptions)
```

### Parameters

- `options`: An Object containing properties derived from the FormControl.fieldOptions results (disabled, invalid, readOnly, required) to determine the styling of the Checkbox state. An id and optional name property that accept a String value which connect Accessibility properties from the label to the Checkbox. A checked and optional indeterminate property that accept a Boolean value to determine the styling of the Checkbox state. A value property that accepts a String for form submission.

### Returns

An Object matching the [Anatomy](#anatomy) containing `className` and a11y properties relevant to each element/section. Additionally, it returns an `iconOptions` Object to use as the parameters for the Icon API.

## Behavior

### Layout

**Do** use multiple Checkboxes in a vertical list.

**Don't** use multiple Checkboxes in a single row.

**Do** provide a Label with a Checkbox.

**Don't** use a Checkbox inline without a Label.

### Usage

**Do** use a Checkbox for giving users an option to "opt-in" to a choice.

**Don't** use a Checkbox for showing/hiding content.

### Interactions

**Do** use the indeterminate state for a parent option of a nested group.

**Don't** use the indeterminate state for a single option within a group.

## Accessibility

The Pando Checkbox, and Icon APIs combined with semantic HTML allow the Checkbox to be fully accessible and screen-readable.
