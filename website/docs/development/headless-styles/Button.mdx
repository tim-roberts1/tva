---
sidebar_position: 3
tags: [Development, Packages, Components, Headless Styles, Button, Chakra]
---

<!-- TODO: As of 2.0.0-beta.17 live-codeblock do not support imports. -->
<!-- Keep an eye out so we can kill the components we are importing in. -->
<!-- https://docusaurus.io/docs/markdown-features/code-blocks#imports -->

import CodeBlock from '@theme/CodeBlock'
import BasicButton from '@site/src/components/Button/BasicButton'
import ButtonSizes from '@site/src/components/Button/ButtonSizes'
import ButtonWithIcon from '@site/src/components/Button/ButtonWithIcon'
import DangerButton from '@site/src/components/Button/DangerButton'
import {
  BasicButtonPreview,
  BasicButtonFullPreview,
} from '@site/src/components/Button/BasicButton.preview'
import {
  ButtonSizesPreview,
  ButtonSizesFullPreview,
} from '@site/src/components/Button/ButtonSizes.preview'
import {
  ButtonWithIconPreview,
  ButtonWithIconFullPreview,
} from '@site/src/components/Button/ButtonWithIcon.preview'
import {
  DangerButtonPreview,
  DangerButtonFullPreview,
} from '@site/src/components/Button/DangerButton.preview'
import ViewSourceLink from '@site/src/components/ViewSourceLink/ViewSourceLink'
import LiveExampleTabs from '@site/src/components/LiveExampleTabs/LiveExampleTabs'
import ReleaseNote from '@site/src/components/Admonitions/_hs-release-note.mdx'

<ReleaseNote />

The Button is used to perform an action or event, such as submitting a form, opening a dialog, or using a cancel action.

:::note

Buttons **should not be used for inline text links or outbound links**.
This API is meant for action-based click selections.
For displaying links, use the [Text Link API](./TextLink.mdx).

:::

<ViewSourceLink
  icon="github"
  href="https://github.com/pluralsight/tva/tree/main/packages/headless-styles/src/components/Button"
>
  View source
</ViewSourceLink>

## Import

```javascript
import {
  getButtonProps,
  getDangerButtonProps,
} from '@pluralsight/headless-styles'
```

- **getButtonProps**: The styles for any button outside of status related
- **getDangerButtonProps**: The styles for danger status buttons

## Usage

### Button kinds

`getButtonProps` comes with five kinds to change the visual style of a button. You can use `text` (default), `textWeak`, `weak`, `medium`, and `strong`.

<BasicButton />
<LiveExampleTabs
  fullCode={<BasicButtonFullPreview />}
  sandboxList={{
    chakra: 'basic-button-chakra-i02ytr',
    joy: 'basic-buttons-mui-bnzx9l',
    styled: 'basic-button-css-in-js-sc-mbrwzb',
    svelte: 'basic-button-svelte-4qu65y',
  }}
>
  <BasicButtonPreview />
</LiveExampleTabs>

### Button sizes

Use the `size` options to change the size of a button. The options are: `xs`, `s`, `m` (default), `l`.

<ButtonSizes />
<LiveExampleTabs
  fullCode={<ButtonSizesFullPreview />}
  sandboxList={{
    chakra: 'basic-button-chakra-i02ytr',
    joy: 'basic-buttons-mui-bnzx9l',
    styled: 'basic-button-css-in-js-sc-mbrwzb',
    svelte: 'basic-button-svelte-4qu65y',
  }}
>
  <ButtonSizesPreview />
</LiveExampleTabs>

:::tip

We use the standard clothing size shorthand values for all `size` related options in any library we provide (i.e. xs-xxl).

:::

### Button with icon

You can add icons to the start or end of a button using the `getButtonWithIconProps` and [getIconProps](./Icon.mdx) helpers with an Icon from the [Icons](../icons/intro) library.

<ButtonWithIcon />
<LiveExampleTabs
  fullCode={<ButtonWithIconFullPreview />}
  sandboxList={{
    chakra: 'button-with-icon-chakra-2st397',
    joy: 'button-with-icon-mui-lg9n42',
    styled: 'button-with-icon-css-in-js-sc-x7lvu5',
    svelte: 'button-with-icon-svelte-0p0jv0',
  }}
>
  <ButtonWithIconPreview />
</LiveExampleTabs>

:::tip

For buttons with _only_ an icon and no visible text, use the [Icon Button (getIconButtonProps)](./IconButton.mdx) helper

:::

### Danger Button

`getDangerButtonProps` allows to style a button with a danger theme. The `kind` options are: `text` (default), `medium`, and `strong`.

<DangerButton />
<LiveExampleTabs
  fullCode={<DangerButtonFullPreview />}
  sandboxList={{
    chakra: 'basic-button-chakra-i02ytr',
    joy: 'basic-buttons-mui-bnzx9l',
    styled: 'basic-button-css-in-js-sc-mbrwzb',
    svelte: 'basic-button-svelte-4qu65y',
  }}
>
  <DangerButtonPreview />
</LiveExampleTabs>

## Extending

There are times you may need to make a slight adjustment to the button styles which is easy to do since we are giving you an Object.

Here is an example of using `styled-components` to extend a button text color.

```jsx title=page/Login/components/CustomButton.jsx
import styled from 'styled-components'
import { getJSButtonProps } from '@pluralsight/headless-styles'

const CustomButton = styled.button`
  ${getJSButtonProps({ kind: 'medium' }).cssProps}
  color: '#bfbfbf'
`

export default CustomButton
```

:::tip

When combining `headless-styles` with other libraries, please follow the guides in the example codesandboxes for the best experience. **Some libraries require special support**.

:::

## API

| Name                             | Argument Type                                            | Default                               | Description                                                          |
| -------------------------------- | -------------------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------- |
| `getButtonProps`                 | **options**: [ButtonOptions](#buttonoptions)             | **kind**: 'text', <br />**size**: 'm' | Get main button styles.                                              |
| `getDangerButtonProps`           | **options**: [DangerButtonOptions](#dangerbuttonoptions) | **kind**: 'text', <br />**size**: 'm' | Get danger button styles.                                            |
| `getJSButtonProps`               | **options**: [ButtonOptions](#buttonoptions)             | same as CSS API                       | CSS-in-JS API                                                        |
| `getJSDangerButtonProps`         | **options**: [DangerButtonOptions](#dangerbuttonoptions) | same as CSS API                       | CSS-in-JS API                                                        |
| `getButtonWithIconProps`         | **options**: [ButtonOptions](#buttonoptions)             | **kind**: 'text', <br />**size**: 'm' | Get main button styles plus [iconOptions](./Icon.mdx#iconoptions).   |
| `getDangerButtonWithIconProps`   | **options**: [DangerButtonOptions](#dangerbuttonoptions) | **kind**: 'text', <br />**size**: 'm' | Get danger button styles plus [iconOptions](./Icon.mdx#iconoptions). |
| `getJSButtonWithIconProps`       | **options**: [ButtonOptions](#buttonoptions)             | same as CSS API                       | CSS-in-JS API plus [iconOptions](./Icon.mdx#iconoptions).            |
| `getJSDangerButtonWithIconProps` | **options**: [DangerButtonOptions](#dangerbuttonoptions) | same as CSS API                       | CSS-in-JS API plus [iconOptions](./Icon.mdx#iconoptions).            |

## Options

### ButtonOptions

This interface is available as an `import` for use.

```typescript
interface ButtonOptions
  extends DetailedHTMLProps<
    ButtonHTMLAttributes<HTMLButtonElement>,
    HTMLButtonElement
  > {
  kind?: 'text' | 'text-weak' | 'weak' | 'medium' | 'strong'
  size?: 'xs' | 's' | 'm' | 'l'
  tech?: 'svelte'
}
```

```typescript title="Import example"
import type { ButtonOptions } from '@pluralsight/headless-styles/types'
```

### DangerButtonOptions

```typescript
interface DangerButtonOptions extends ButtonOptions {
  kind?: 'text' | 'medium' | 'strong'
  size?: 'xs' | 's' | 'm' | 'l'
  tech?: 'svelte'
}
```

## Props

### ButtonProps

```typescript
interface ButtonProps {
  className: string
  type: 'button'
}
```

```js title="Return value example"
{
  className: `ps-btn ${...internalStyles}`
  type: 'button'
}
```

### JSButtonProps

```typescript
interface JSButtonProps {
  cssProps: TemplateLiteralStringArray | string
  styles: Record<CSSProps, string>
  type: ButtonType
}
```

```js title="Return value example"
{
  cssProps: `
    color: #fff;
    background-color: #000;
    ...
  `,
  styles: {
    color: '#fff',
    backgroundColor: '#000'
    '&:hover': {
      ...
    }
  },
  type: 'button',
}
```

### ButtonWithIconProps

```typescript
interface ButtonWithIconProps {
  button: ButtonProps
  iconOptions: IconOptions
}
```

### JSButtonWithIconProps

```typescript
interface JSButtonWithIconProps {
  button: JSButtonProps
  iconOptions: IconOptions
}
```

## Guides

### Button size guide

Button sizes are relative to the `padding` and `font-size` styles. Here is an interface that gives the sizing guide. Variables are referenced from the [`tokens`](../tokens/intro.md) package.

```typescript title="Example interface - does not exist in types"
interface ButtonSizeGuide {
  xs: {
    fontSize: '0.75rem'
    paddingInlineStart: '0.5rem'
    paddingInlineEnd: '0.5rem'
  }
  s: {
    fontSize: '0.875rem'
    paddingInlineStart: '0.75rem'
    paddingInlineEnd: '0.75rem'
  }
  m: {
    fontSize: '1rem'
    paddingInlineStart: '1rem'
    paddingInlineEnd: '1rem'
  }
  l: {
    fontSize: '1rem'
    paddingInlineStart: '1.5rem'
    paddingInlineEnd: '1.5rem'
  }
}
```

### Button color guide

Button colors are relative to the `background` style and have a direct correlation from the `kind` to the [`design-tokens`](../tokens/intro.md) package. Here is an interface that gives the color guide.

```typescript title="Example interface - does not exist in types"
interface ButtonColorGuide {
  text: 'transparent'
  'text-weak': 'transparent'
  weak: 'var(--ps-neutral-background)'
  medium: 'var(--ps-background)'
  strong: 'var(--ps-background-weak)'
}

interface DangerButtonColorGuide {
  text: 'transparent'
  medium: 'var(--ps-danger-background)'
  strong: 'var(--ps-danger-background-weak)'
}
```
