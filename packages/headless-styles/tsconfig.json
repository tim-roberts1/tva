{
  "extends": "../../tsconfig.json",
  "include": ["src", "tests"],
  "exclude": ["sandbox", "plugins"],
  "compilerOptions": {
    "allowJs": true,
    "baseUrl": "src",
    "isolatedModules": false
  }
}
