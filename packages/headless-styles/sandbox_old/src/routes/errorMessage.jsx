import ErrorMessage from '../components/ErrorMessage'

const errorMessageRoute = {
  element: <ErrorMessage logJS />,
  label: 'Error Message',
  path: '/error-message',
}

export default errorMessageRoute
